<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elysian | Admin Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body { background: #0f172a; height: 100vh; display: flex; align-items: center; justify-content: center; }
        .login-card { width: 400px; padding: 2rem; background: white; border-radius: 0; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5); }
        .btn-dark { border-radius: 0; background: #0f172a; }
    </style>
</head>
<body>
    <div class="login-card">
        <h2 class="text-center mb-4 font-serif">ELYSIAN</h2>
        <p class="text-muted text-center small mb-4">MANAGEMENT PORTAL</p>
        
        <% if (locals.error) { %>
            <div class="alert alert-danger py-2 small"><%= error %></div>
        <% } %>

        <form action="/login" method="POST">
            <div class="mb-3">
                <label class="form-label small fw-bold">USERNAME</label>
                <input type="text" name="username" class="form-control rounded-0" required autofocus>
            </div>
            <div class="mb-4">
                <label class="form-label small fw-bold">PASSWORD</label>
                <input type="password" name="password" class="form-control rounded-0" required>
            </div>
            <button type="submit" class="btn btn-dark w-100 py-2">AUTHENTICATE</button>
        </form>
    </div>
</body>
</html>
